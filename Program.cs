using System;

namespace PracticeAlgorithm
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            TimeConversionSolution.TimeMain();
        }

        //has to be completed
        static void BinarySearch(int targetNum)
        {
            int[] testArray = new int[6] { -1, 0, 3, 5, 9, 12 };

            int mid = testArray.Length / 2;
            if (targetNum > testArray[mid])
            {

            }
        }
    }
}
